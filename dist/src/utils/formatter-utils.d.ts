/**
 * formatApiFileName()
 *
 * formats the api file name.
 * @param domainName the domain name.
 * @returns the formatted api file name
 */
export declare const formatApiFileName: (domainName: string) => string;
/**
 * formatClassName()
 *
 * formats a string to be suitabble for a class name.
 * @param dirty the string to format.
 * @returns the formatted thing.
 */
export declare const formatClassName: (dirty: string) => string;
/**
 * formatDirectoryOrFileName()
 *
 * formats a string to be suitable as a file or a directory name.
 * @param dirty the string to format.
 * @returns the formatted string.
 */
export declare const formatDirectoryOrFileName: (dirty: string) => string;
/**
 * formatDomainDirectoryName()
 *
 * formats the domain directory name from the domain name.
 * @param domainname the domain name.
 * @returns the formatted domain directory name.
 */
export declare const formatDomainDirectoryName: (domainname: string) => string;
/**
 * formatEventStoreFileName()
 *
 * formats the event store file name for the specified domain name.
 * @param domainName the domain name
 * @returns the event store file name.
 */
export declare const formatEventStoreFileName: (domainName: string) => string;
/**
 * formatLogInfo()
 *
 * formats the log as informational log.
 * @param message the message to format.
 * @returns the formatted message.
 */
export declare const formatLogInfo: (message: string) => string;
/**
 * formatLogError()
 *
 * formats the log as an error log.
 * @param message the message to format.
 * @returns the formatted message.
 */
export declare const formatLogError: (message: string) => string;
/**
 * formatModuleFileName()
 *
 * formats a string to an appropriate module file name.
 * @param moduleName the name of the module.
 * @returns the formatted module file name.
 */
export declare const formatModuleFileName: (moduleName: string) => string;
/**
 * formatTitleText()
 *
 * formats a string to be suitabble for title text.
 * @param dirty the string to format.
 * @returns the formatted thing.
 */
export declare const formatTitleText: (dirty: string) => string;
/**
 * formatVariableName()
 *
 * formats the string for use as a variable name.
 * @param dirty the string to format.
 * @returns a string formatted for use as a variable name.
 */
export declare const formatVariableName: (dirty: string) => string;
//# sourceMappingURL=formatter-utils.d.ts.map